##  Habit Tracker — трекер полезных привычек

Backend-часть SPA-приложения (Django 5.2, DRF, PostgreSQL, Celery, Redis)

## 🎯 Назначение проекта

Проект позволяет пользователям создавать полезные и приятные привычки, получать автоматические напоминания в Telegram, вести свой список привычек и просматривать публичные привычки других пользователей.

## 🚀 Функционал
👤 Пользователи

Регистрация по email

JWT-авторизация (access + refresh)

Профиль пользователя (c telegram_chat_id)

##  ✔️ Привычки

Создание привычек

Редактирование

Удаление

Просмотр своих привычек

Просмотр публичных привычек

Пагинация по 5 элементов

Валидация по правилам «Атомных привычек»

## 🔔 Telegram-уведомления

Связка Telegram аккаунта по chat_id

Автоматическая отправка напоминаний через Celery

Redis используется как брокер и backend

##  🧩 Модели
Habit
Поле	Описание
user	Пользователь
place	Где выполнять
time	Когда выполнять
action	Что сделать
is_pleasant	Это приятная привычка?
related_habit	Связанная приятная привычка
periodicity	1–7 дней
reward	Вознаграждение
duration	≤ 120 секунд
is_public	Публичная ли привычка

## 🔐 Валидации:

Нельзя одновременно иметь reward и related_habit

duration ≤ 120

related_habit может быть только приятной

Приятной привычке нельзя указывать reward

periodicity 1–7

## 🔑 Эндпоинты API
Пользователи
Регистрация

POST /api/users/register/

Авторизация

POST /api/users/token/
POST /api/users/token/refresh/

Профиль

GET /api/users/profile/<id>/
PATCH /api/users/profile/<id>/

Привычки
Личные привычки

GET /api/habits/
POST /api/habits/

Работа с конкретной привычкой

GET /api/habits/<id>/
PUT /api/habits/<id>/
PATCH /api/habits/<id>/
DELETE /api/habits/<id>/

Публичные привычки

GET /api/habits/public/

✅ Фактические рабочие URL в твоём проекте

Ты сам это уже проверил, у тебя зарегистрированы ровно эти роуты:

🔹 Swagger UI
http://<IP>:9000/api/docs/swagger/


пример:

http://158.160.65.109:9000/api/docs/swagger/

🔹 Redoc
http://<IP>:9000/api/docs/redoc/


пример:

http://158.160.65.109:9000/api/docs/redoc/

🔹 OpenAPI schema (JSON)
http://<IP>:9000/api/schema/


пример:

http://158.160.65.109:9000/api/schema/



## 📬 Telegram интеграция
Получить chat_id:

Написать боту @userinfobot

Получить свой chat_id

Записать в профиле:
PATCH /api/users/profile/<id>/ {"telegram_chat_id": "xxxxx"}

Отправка уведомлений происходит автоматически:

каждый 1 минуту

через Celery Beat

## 🔧 Переменные окружения (.env)
SECRET_KEY=...
DEBUG=True
DB_NAME=habit_tracker_db
DB_USER=postgres
DB_PASSWORD=...
DB_HOST=localhost
DB_PORT=5432
CORS_ALLOWED_ORIGINS=http://localhost:3000

## 🧪 Тесты

✔ Покрытие тестами → 88%
✔ Использованы pytest, pytest-django, pytest-cov
✔ Тестируются:

API пользователей

API привычек

Валидаторы

Запуск тестов:

pytest --cov=habits --cov=users --cov-report=term-missing

## 🔍 Стиль и качество кода

✔ PEP8
✔ flake8 — 100% (кроме миграций)
✔ Чистая структура проекта
✔ drf-spectacular — документация по API


## 🏁 Развёртывание

## Установка Redis (Windows)

1. Скачать Redis for Windows:
   https://github.com/tporadowski/redis/releases

2. Распаковать в: C:\Program Files\Redis

3. Запустить через терминал и указать путь : пример "C:\Program Files\Redis\redis-server.exe"
   redis-server.exe

4. Проверить:
   redis-cli.exe ping
   → PONG

poetry install
python manage.py migrate
python manage.py runserver

## ⏱ Celery & Redis

Celery Worker:

celery -A config worker -l INFO --pool=solo


Celery Beat:

celery -A config beat -l INFO

🐳 Запуск проекта в Docker

Полностью автоматизированный запуск Habit Tracker (Django + PostgreSQL + Redis + Celery + Celery Beat).

📁 Требования

Перед запуском установите:

Docker

Docker Compose

🚀 Полный запуск
1. Собрать контейнеры
docker compose build

2. Запустить проект
docker compose up


После запуска доступны сервисы:

Сервис	Порт	Описание
Backend (Django)	9000	API сервер
PostgreSQL	5432	База данных
Redis	6379	брокер Celery
Celery Worker	—	обработка задач
Celery Beat	—	периодические задачи

API доступно по адресу:

http://localhost:9000/api/

🔄 Перезапуск (без пересборки)
docker compose down
docker compose up

🧹 Полный сброс контейнеров и данных PostgreSQL

(удаляет том базы)

docker compose down -v
docker compose up --build

🧪 Запуск тестов внутри контейнера
docker exec -it habit_backend sh -c "pytest --ds=config.settings"

🐚 Вход в контейнер Django
docker exec -it habit_backend sh

📦 Пример структуры docker-compose.yaml (используемой в проекте)
backend → порт 9000  
celery → worker  
celery_beat → scheduler  
redis → брокер  
postgres → база данных


## 🔄 CI/CD (GitHub Actions)

В проекте настроен полный CI/CD pipeline с использованием GitHub Actions.

### 📁 Workflow
Файл workflow расположен в репозитории:

.github/workflows/deploy.yml

### 🔍 Этапы pipeline

1. **Flake8 lint**
   - Проверка кода на соответствие PEP8
   - Ошибки линтера останавливают pipeline

2. **Tests**
   - Автоматический запуск тестов Django
   - Используются PostgreSQL и Redis как сервисы GitHub Actions
   - Выполняются миграции перед тестами
   - Ошибки тестов блокируют деплой

3. **Deploy**
   - Запускается только после успешного lint и tests
   - Подключение к серверу по SSH (через GitHub Secrets)
   - Автоматический `git pull`
   - Пересборка и перезапуск Docker-контейнеров
   - Сборка статических файлов (`collectstatic`)

### 🔐 Используемые GitHub Secrets

- `SERVER_HOST` — IP сервера
- `SERVER_USER` — пользователь сервера
- `SERVER_SSH_KEY` — приватный SSH-ключ

### 🚀 Результат

При каждом push в ветку `develop`:
- код автоматически проверяется линтером
- запускаются тесты
- при успешной проверке проект деплоится на сервер


## 📦 Стек технологий

Python 3.12

Django 5.2

Django REST Framework

PostgreSQL

Redis

Celery

pytest

drf-spectacular

## 📤 GitHub

git@github.com:ArtemKabr/Habit_tracker.git
