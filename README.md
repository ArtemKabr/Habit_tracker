### Habit Tracker — трекер полезных привычек

Backend-часть SPA-приложения для трекинга привычек
(Django 5.2, DRF, PostgreSQL, Redis, Celery, Nginx)

### 🎯 Назначение проекта

Проект позволяет пользователям создавать полезные и приятные привычки, получать автоматические напоминания в Telegram, управлять своими привычками и просматривать публичные привычки других пользователей.

### 📦 Стек технологий

Python 3.12

Django 5.2

Django REST Framework

PostgreSQL

Redis

Celery

Nginx

Docker / Docker Compose

GitHub Actions

pytest

drf-spectacular

### 📤 Репозиторий

git@github.com:ArtemKabr/Habit_tracker.git

### 🚀 Функционал
👤 Пользователи

Регистрация по email

JWT-авторизация (access + refresh)

Профиль пользователя с telegram_chat_id

### ✔️ Привычки

Создание / редактирование / удаление привычек

Просмотр личных привычек

Просмотр публичных привычек

Пагинация (по 5 элементов)

Валидация по правилам «Атомных привычек»

### 🔔 Telegram-уведомления

Привязка Telegram-аккаунта по chat_id

Автоматическая отправка напоминаний

Celery + Celery Beat

Redis используется как брокер и backend

### 🧩 Модель Habit
Поле	Описание
user	Пользователь
place	Где выполнять
time	Когда выполнять
action	Что сделать
is_pleasant	Приятная привычка
related_habit	Связанная привычка
periodicity	1–7 дней
reward	Вознаграждение
duration	≤ 120 секунд
is_public	Публичная привычка
### 🔐 Валидации

Нельзя указывать reward и related_habit одновременно

duration ≤ 120 секунд

related_habit может быть только приятной

Приятной привычке нельзя указывать reward

periodicity от 1 до 7 дней

### 🔑 API Endpoints
Пользователи

POST /api/users/register/

POST /api/users/token/

POST /api/users/token/refresh/

GET /api/users/profile/<id>/

PATCH /api/users/profile/<id>/

Привычки

GET /api/habits/

POST /api/habits/

GET /api/habits/<id>/

PUT /api/habits/<id>/

PATCH /api/habits/<id>/

DELETE /api/habits/<id>/

GET /api/habits/public/

### 📘 API документация

Swagger UI
http://130.193.55.90:9000/api/docs/swagger/

Redoc
http://130.193.55.90:9000/api/docs/redoc/

OpenAPI schema
http://130.193.55.90:9000/api/schema/

### 📬 Telegram интеграция

Написать боту @userinfobot

Получить chat_id

Сохранить в профиле пользователя:

PATCH /api/users/profile/<id>/
{
  "telegram_chat_id": "xxxxx"
}


Уведомления отправляются автоматически:

каждые 1 минуту

через Celery Beat

### 🔧 Переменные окружения (.env)
SECRET_KEY=...
DEBUG=True

DB_NAME=habit_tracker_db
DB_USER=postgres
DB_PASSWORD=...
DB_HOST=localhost
DB_PORT=5432

CORS_ALLOWED_ORIGINS=http://localhost:3000

### 🧪 Тесты

Покрытие тестами: 88%

Используются: pytest, pytest-django, pytest-cov

Тестируются:

API пользователей

API привычек

Валидаторы

Запуск локально:

pytest --cov=habits --cov=users --cov-report=term-missing

### 🐳 Docker и контейнеризация

Проект полностью контейнеризирован.

Используемые контейнеры

Django (backend)

PostgreSQL

Redis

Celery Worker

Celery Beat

Nginx (reverse proxy)

Запуск проекта одной командой
docker compose up --build

Доступные сервисы
Сервис	Порт	Описание
Django API	9000	Backend
PostgreSQL	5432	База данных
Redis	6379	Брокер Celery
Nginx	80	Reverse proxy

API доступно по адресу:

http://localhost:9000/api/

### 🔄 CI/CD (GitHub Actions)

В проекте настроен полный CI/CD pipeline.

Workflow

Файл:

.github/workflows/deploy.yml

Этапы pipeline

Lint

flake8

Проверка PEP8

Tests

Django tests

PostgreSQL и Redis как сервисы GitHub Actions

Прогон миграций

Docker build

Проверка сборки Docker-образов

Deploy

Запускается только для ветки main

Деплой через SSH

docker compose down

docker compose up -d --build

collectstatic

GitHub Secrets

SERVER_HOST

SERVER_USER

SERVER_SSH_KEY

### 🚀 Развёрнутый сервер

Проект автоматически разворачивается на сервере:

IP сервера:
http://130.193.55.90:9000